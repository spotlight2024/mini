# Chrome代理配置环境变量
# 使用方法: docker compose --env-file proxy.env up

# 启用代理
PROXY_ENABLED=true

# 淘宝反爬虫代理配置
PROXY_HOST=101.96.145.103
PROXY_PORT=57001
PROXY_USERNAME=vgmpgv
PROXY_PASSWORD=1bk79g9y

# 可选：设置其他Chrome选项
# SE_OPTS=--load-extension=/opt/chrome_extensions/proxy_auth --disable-blink-features=AutomationControlled --no-sandbox
