---
description:
globs:
alwaysApply: false
---
# 开发工作流

## 环境设置
1. 安装Python依赖: `pip install -r script/requirements.txt`
2. 配置虚拟环境: 使用项目根目录的`.venv`
3. 设置环境变量: 参考`.env`文件配置

## 开发流程
1. **功能开发**: 在`script/`目录下开发新功能
2. **单元测试**: 在`script/tests/`目录下编写测试
3. **集成测试**: 使用真实设备进行端到端测试
4. **代码审查**: 确保符合编码规范

## 测试执行
```bash
# 运行所有测试
PYTHONPATH=script pytest script/tests/

# 运行指定测试
PYTHONPATH=script pytest script/tests/test_web_driver.py -k test_real_connect

# 显示详细日志
PYTHONPATH=script pytest script/tests/ -s --log-cli-level=INFO
```

## 服务启动
```bash
# 开发模式启动
cd script && python main.py

# 生产模式启动
cd script && uvicorn server:app --host 0.0.0.0 --port 8000
```

## 调试指南
- 使用日志追踪: 查看`script/logs/`目录
- WebDriver调试: 启用Chrome远程调试端口9222
- 设备连接调试: 使用ADB命令检查设备状态

## 部署流程
1. 代码提交到版本控制
2. 自动化测试通过
3. 构建Docker镜像（如有）
4. 部署到云服务器
5. 健康检查验证

## 监控与维护
- 服务健康检查: `/health`接口
- 设备状态监控: 定期检查设备连接状态
- 日志分析: 分析错误日志和性能指标
- 资源清理: 定期清理过期连接和资源
