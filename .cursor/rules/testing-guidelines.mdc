---
description: 
globs: 
alwaysApply: false
---
# 测试指南

## 测试策略
- **单元测试**: 测试单个函数和类的功能
- **集成测试**: 测试模块间的交互
- **端到端测试**: 测试完整的用户流程
- **性能测试**: 测试系统性能和资源使用

## 测试目录结构
```
script/tests/
├── test_device.py          # 设备管理测试
├── test_web_driver.py      # WebDriver功能测试
├── test_websocket.py       # WebSocket通信测试
└── conftest.py             # 测试配置和fixture
```

## 测试配置
- 测试框架: pytest
- 配置文件: [script/pytest.ini](mdc:script/pytest.ini)
- 测试依赖: 在[script/requirements.txt](mdc:script/requirements.txt)中定义

## 测试执行命令
```bash
# 设置Python路径
export PYTHONPATH=script

# 运行所有测试
pytest script/tests/

# 运行特定测试文件
pytest script/tests/test_web_driver.py

# 运行特定测试方法
pytest script/tests/test_web_driver.py -k test_real_connect

# 显示详细输出
pytest script/tests/ -s

# 显示日志输出
pytest script/tests/ --log-cli-level=INFO
```

## 测试最佳实践
1. **测试隔离**: 每个测试应该独立运行，不依赖其他测试
2. **Mock外部依赖**: 使用mock模拟外部服务调用
3. **测试数据管理**: 使用fixture管理测试数据
4. **断言清晰**: 使用明确的断言消息
5. **覆盖率检查**: 确保关键代码路径被测试覆盖

## 真实设备测试
- 需要本地Chrome/Chromium开启9222端口
- 使用真实Android设备或模拟器
- 测试前确保设备连接正常
- 测试后清理测试数据

## 测试数据管理
- 使用测试专用的配置文件
- 避免在测试中修改生产数据
- 测试完成后清理临时数据
- 使用随机数据避免测试冲突

## 持续集成
- 提交代码时自动运行测试
- 测试失败时阻止代码合并
- 生成测试覆盖率报告
- 定期运行性能测试
