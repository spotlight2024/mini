---
description: 
globs: 
alwaysApply: false
---
# API接口规范

## 基础响应格式
所有API响应都遵循统一格式：
```json
{
  "code": 0,
  "message": "success",
  "data": {},
  "error": null,
  "trace_id": "uuid-string"
}
```

## 核心接口

### 设备连接管理
- **POST /connect**: 连接Android设备
  - 请求: `{"serial_id": "设备序列号"}`
  - 响应: 连接状态

- **POST /disconnect**: 断开设备连接
  - 请求: `{"serial_id": "设备序列号"}`
  - 响应: 断开状态

### 元素操作
- **POST /find_element**: 查找页面元素
  - 请求: `{"serial_id": "设备序列号", "method": "定位方法", "selector": "选择器"}`
  - 定位方法: "id", "xpath", "css selector", "name", "class name", "tag name", "link text", "partial link text"

- **POST /action**: 执行基础操作
  - 请求: `{"serial_id": "设备序列号", "type": "操作类型", "params": {}}`
  - 操作类型: "click", "findElement"

### 操作序列
- **POST /run_operations**: 执行操作序列
  - 请求: `{"serial_id": "设备序列号", "operations": [操作列表]}`
  - 支持操作类型: "findElement", "click", "wait", "js", "handlePopup"

## 错误码规范
- `0`: 成功
- `1001`: 连接失败
- `1002`: 设备未找到
- `1003`: 元素未找到
- `2000`: 系统异常
- `400101`: 设备未找到（操作序列）

## 接口实现
主要接口实现在: [script/server.py](mdc:spot_light/script/server.py)

## 请求/响应模型
使用Pydantic模型定义，参考:
- ConnectRequest
- ActionRequest
- FindElementRequest
- OperationRequest
- APIResponse

## 安全考虑
- 输入验证和清理
- 请求频率限制
- 设备访问权限控制
- 敏感信息保护
